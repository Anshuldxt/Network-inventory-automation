# Phase 2 patch notes

- Added Outlook shared-mailbox attachment sync for Huawei, Ericsson and ZTE.
- Added editable `auto_sync_config.json` with the configured OneDrive Input path and fixed filters.
- Added manifest-based duplicate detection and atomic attachment replacement.
- Added GUI `Sync Now` action.
- Added daily 10:00 Task Scheduler installer scripts.
- Added Windows `pywin32` dependency and EXE-safe config/database paths.
- Power BI remains outside this phase.
